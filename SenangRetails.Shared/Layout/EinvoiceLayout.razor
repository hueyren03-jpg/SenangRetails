@* @using System.Diagnostics
@using SenangRetails.Shared.Pages
@inherits LayoutComponentBase
@using SenangRetails.Shared.Services
@inject EInvoiceSetupGuideService EInvoiceSetupGuideService

<div class="page">
    <div class="sidebar">
        <NavMenu @ref="navMenuRef" OnStatusChanged="StateHasChanged" />
    </div>

    <main class="@(navMenuRef?.IsExpanded == true ? "main-expanded" : "main-collapsed")">
        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>


<EInvoiceSetupGuide IsVisible="EInvoiceSetupGuideService.IsOpen"
                    OnClose="EInvoiceSetupGuideService.Close" />
@code {
    protected override void OnInitialized()
    {
        EInvoiceSetupGuideService.OnChange += StateHasChanged;
    }

    public void Dispose()
    {
        EInvoiceSetupGuideService.OnChange -= StateHasChanged;
    }

    private NavMenu? navMenuRef;
} *@
