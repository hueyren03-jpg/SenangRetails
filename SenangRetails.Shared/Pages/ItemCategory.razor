@page "/itemcategory"
@using System
@using System.Collections.Generic
@using System.Linq
@using System.Threading.Tasks
@using SenangRetails.Shared.Models.DTOs
@using SenangRetails.Shared.Services
@using SenangRetails.Shared.Services.ItemCategoryService
@using SenangRetails.Shared.Services.ItemDepartmentService
@inject IItemCategoryService ItemCategoryService
@inject IItemDepartmentService ItemDepartmentService
@inject LanguageService LangSvc
@inject NavigationManager Navigation
@inject IJSRuntime JS

<div class="item-category-page">

    <!-- Page Header -->
    <div class="section-header">
        <div class="header-left">
            <div class="header-nav-row">
                @if (ShowRestoreButton)
                {
                    <button class="mobile-menu-btn" @onclick="TriggerToggleSidebar" title="Toggle navigation menu">
                        <i class="fa-solid fa-bars"></i>
                    </button>
                }
                <button class="back-button" @onclick='() => Navigation.NavigateTo("/menu")'>
                    <i class="fa-solid fa-arrow-left"></i>
                    <span>@LangSvc.GetText("Back")</span>
                </button>
            </div>
            <h2>@LangSvc.GetText("ItemCategory")</h2>
            <p>Organize products into categories and link each category to an item department.</p>
        </div>

        <div class="header-right">
            <button type="button" class="add-btn" @onclick="OpenAddModal">
                <i class="fa-solid fa-plus"></i>
                <span>Add Category</span>
            </button>
        </div>
    </div>

    <!-- Main Card Container -->
    <div class="rules-container">

        <div class="section-title-row-with-action">
            <h3>Category Records</h3>
            <span class="rule-count-text">@FilteredCategories.Count() of @categories.Count categories</span>
        </div>

        <!-- Search & Filter Toolbar -->
        <div class="search-toolbar-row">
            <div class="staff-search-wrap">
                <i class="fa-solid fa-magnifying-glass staff-search-icon"></i>
                <input type="text"
                       class="staff-search-input"
                       placeholder="Search category code, name or department..."
                       @bind="searchQuery"
                       @bind:event="oninput" />
                @if (!string.IsNullOrEmpty(searchQuery))
                {
                    <button type="button" class="search-clear-btn" @onclick='() => searchQuery = ""'>✕</button>
                }
            </div>

            <div class="filter-controls-wrap">
                <!-- Department Filter Dropdown -->
                <select class="department-filter-select" @bind="departmentFilter">
                    <option value="">All Departments</option>
                    @foreach (var dept in departments)
                    {
                        <option value="@dept.Id">@dept.Name</option>
                    }
                </select>

                <!-- Status Filter Pills -->
                <div class="filter-pills-wrap">
                    <button type="button" class="@PillClass("All")" @onclick='() => statusFilter = "All"'>
                        All (@categories.Count)
                    </button>
                    <button type="button" class="@PillClass("Active")" @onclick='() => statusFilter = "Active"'>
                        Active (@categories.Count(d => d.Active))
                    </button>
                    <button type="button" class="@PillClass("Inactive")" @onclick='() => statusFilter = "Inactive"'>
                        Inactive (@categories.Count(d => !d.Active))
                    </button>
                </div>
            </div>
        </div>

        @if (isLoading)
        {
            <div class="loading-state">
                <div class="loading-spinner"></div>
                <span>Loading item categories...</span>
            </div>
        }
        else if (!FilteredCategories.Any())
        {
            <div class="empty-state">
                <div class="empty-icon-wrap">
                    <i class="fa-solid fa-tags"></i>
                </div>
                <h4>No item categories found</h4>
                @if (!string.IsNullOrEmpty(searchQuery) || !string.IsNullOrEmpty(departmentFilter) || statusFilter != "All")
                {
                    <p>No results match your search or filter criteria.</p>
                    <button type="button" class="btn-clear-filters" @onclick="ClearFilters">Clear Filters</button>
                }
                else
                {
                    <p>Start organizing your catalog by adding your first item category.</p>
                    <button type="button" class="add-btn empty-action-btn" @onclick="OpenAddModal">
                        <i class="fa-solid fa-plus"></i> Add Category
                    </button>
                }
            </div>
        }
        else
        {
            <div class="list-container">
                <div class="list-header">
                    <span class="align-left sortable-cell" @onclick='() => SortBy("Code")'>
                        Category Code
                        @if (sortColumn == "Code") { <span class="sort-caret">@(sortAscending ? "▲" : "▼")</span> }
                    </span>
                    <span class="align-left sortable-cell" @onclick='() => SortBy("Name")'>
                        Category Name
                        @if (sortColumn == "Name") { <span class="sort-caret">@(sortAscending ? "▲" : "▼")</span> }
                    </span>
                    <span class="align-left sortable-cell" @onclick='() => SortBy("Department")'>
                        Department
                        @if (sortColumn == "Department") { <span class="sort-caret">@(sortAscending ? "▲" : "▼")</span> }
                    </span>
                    <span class="align-center sortable-cell" @onclick='() => SortBy("Active")'>
                        Active
                        @if (sortColumn == "Active") { <span class="sort-caret">@(sortAscending ? "▲" : "▼")</span> }
                    </span>
                    <span class="align-center">Actions</span>
                </div>

                @foreach (var item in FilteredCategories)
                {
                    <div class="@RowClass(item)" @onclick="() => OpenEditModal(item)">

                        <span class="cell align-left" data-label="Category Code">
                            <span class="cat-code-badge">@item.Code</span>
                        </span>

                        <span class="cell align-left" data-label="Category Name">
                            <span class="cat-desc-text">@item.Name</span>
                        </span>

                        <span class="cell align-left" data-label="Department">
                            @if (!string.IsNullOrWhiteSpace(item.DepartmentName))
                            {
                                <span class="department-tag">
                                    <i class="fa-solid fa-building tag-icon"></i>
                                    @item.DepartmentName
                                </span>
                            }
                            else
                            {
                                <span class="no-department-tag">—</span>
                            }
                        </span>

                        <span class="cell align-center" data-label="Active" @onclick:stopPropagation>
                            <div class="active-cell-wrap">
                                <div class="@TickClass(item.Active)" @onclick="() => ToggleItemActive(item)" title="Toggle active">
                                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                                        <polyline points="20 6 9 17 4 12"></polyline>
                                    </svg>
                                </div>
                                <span class="@BadgeClass(item.Active)">@(item.Active ? "Active" : "Inactive")</span>
                            </div>
                        </span>

                        <span class="cell align-center" data-label="Actions" @onclick:stopPropagation>
                            <div class="action-buttons-wrap">
                                <button type="button" class="pt-action-btn" title="Edit" @onclick="() => OpenEditModal(item)">
                                    <i class="fa-solid fa-pen-to-square"></i>
                                </button>
                                <button type="button" class="pt-action-btn pt-delete-btn" title="Delete" @onclick="() => RequestDelete(item)">
                                    <i class="fa-solid fa-trash"></i>
                                </button>
                            </div>
                        </span>

                    </div>
                }
            </div>
        }
    </div>
</div>

<!-- Add / Edit Modal -->
@if (showModal)
{
    <div class="modal-overlay" @onclick="CloseModal">
        <div class="category-modal-box" @onclick:stopPropagation>

            <div class="modal-header">
                <div class="modal-title-wrap">
                    <div class="modal-icon-badge">
                        <i class="fa-solid fa-tags"></i>
                    </div>
                    <h3>@(isEditing ? "Edit Category" : "Add Category")</h3>
                </div>
                <button type="button" class="modal-close-btn" @onclick="CloseModal" aria-label="Close">✕</button>
            </div>

            <div class="modal-body">
                @if (!string.IsNullOrEmpty(formError))
                {
                    <div class="form-error-banner">
                        <i class="fa-solid fa-circle-exclamation"></i>
                        <span>@formError</span>
                    </div>
                }

                <div class="form-group">
                    <label class="form-label" for="catCode">
                        Category Code
                        <span class="locked-badge">
                            <i class="fa-solid fa-lock"></i> @(isEditing ? "Locked" : "Auto")
                        </span>
                    </label>
                    <div class="locked-input-wrap">
                        <input type="text"
                               id="catCode"
                               class="form-control locked-control"
                               value="@(isEditing && !string.IsNullOrEmpty(currentModel.Code) ? currentModel.Code : "Auto-generated on save")"
                               readonly
                               disabled />
                        <i class="fa-solid fa-lock locked-icon-addon"></i>
                    </div>
                    <span class="form-hint">System-generated unique identifier assigned by the server (cannot be edited)</span>
                </div>

                <div class="form-group">
                    <label class="form-label" for="catName">Category Name <span class="required">*</span></label>
                    <input type="text"
                           id="catName"
                           class="form-control"
                           placeholder="e.g. Toys, Facial, Beverages"
                           maxlength="255"
                           @bind="currentModel.Name" />
                    <span class="form-hint">Name of the category (saved to SupportingTableName)</span>
                </div>

                <div class="form-group">
                    <label class="form-label" for="catDepartment">Item Department</label>
                    <select id="catDepartment" class="form-control" @bind="currentModel.DepartmentId">
                        <option value="">-- None (No Department) --</option>
                        @foreach (var dept in departments)
                        {
                            <option value="@dept.Id">@dept.Name</option>
                        }
                    </select>
                    <span class="form-hint">Assign this category to an item department (saved to ParentID)</span>
                </div>

                <div class="form-group">
                    <div class="active-toggle-row" @onclick="ToggleFormActive">
                        <div class="@FormTickClass">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                                <polyline points="20 6 9 17 4 12"></polyline>
                            </svg>
                        </div>
                        <div class="checkbox-label-text">
                            <span class="label-primary">Active</span>
                            <span class="label-secondary">Enable this category for use in catalog</span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn-cancel" @onclick="CloseModal" disabled="@isSaving">Cancel</button>
                <button type="button" class="btn-save" @onclick="SaveCategory" disabled="@isSaving">
                    @if (isSaving)
                    {
                        <span class="spinner-inline"></span>
                        <span>Saving...</span>
                    }
                    else
                    {
                        <span>Save</span>
                    }
                </button>
            </div>
        </div>
    </div>
}

<!-- Delete Confirmation Modal -->
@if (showDeleteConfirm && itemToDelete != null)
{
    <div class="modal-overlay" @onclick="CancelDelete">
        <div class="category-modal-box confirm-modal-box" @onclick:stopPropagation>

            <div class="modal-header confirm-header">
                <div class="delete-icon-badge">
                    <i class="fa-solid fa-triangle-exclamation"></i>
                </div>
                <h3>Delete Category</h3>
            </div>

            <div class="modal-body confirm-body">
                <p>Are you sure you want to delete this category?</p>
                <div class="delete-item-preview">
                    <span class="cat-code-badge">@itemToDelete.Code</span>
                    <strong>@itemToDelete.Name</strong>
                    @if (!string.IsNullOrWhiteSpace(itemToDelete.DepartmentName))
                    {
                        <span class="department-tag" style="margin-left: 6px;">@itemToDelete.DepartmentName</span>
                    }
                </div>
                <p class="warning-note">This action cannot be undone.</p>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn-cancel" @onclick="CancelDelete" disabled="@isDeleting">Cancel</button>
                <button type="button" class="btn-delete-confirm" @onclick="ConfirmDelete" disabled="@isDeleting">
                    @if (isDeleting)
                    {
                        <span class="spinner-inline"></span>
                        <span>Deleting...</span>
                    }
                    else
                    {
                        <span>Delete</span>
                    }
                </button>
            </div>
        </div>
    </div>
}

<!-- Notification Toast -->
@if (!string.IsNullOrEmpty(notificationMessage))
{
    <div class="@ToastClass">
        <i class="fa-solid @ToastIcon"></i>
        <span>@notificationMessage</span>
    </div>
}

@code {
    [Parameter] public EventCallback OnToggleSidebar { get; set; }
    [Parameter] public bool ShowRestoreButton { get; set; } = false;

    private List<ItemCategoryModel> categories = new();
    private List<ItemDepartmentModel> departments = new();
    private bool isLoading = true;

    private string searchQuery = "";
    private string statusFilter = "All";
    private string departmentFilter = "";
    private string sortColumn = "Name";
    private bool sortAscending = true;

    private bool showModal = false;
    private bool isEditing = false;
    private ItemCategoryModel currentModel = new();
    private string formError = "";
    private bool isSaving = false;

    private bool showDeleteConfirm = false;
    private ItemCategoryModel? itemToDelete = null;
    private bool isDeleting = false;

    private string notificationMessage = "";
    private string notificationType = "success";

    // ── CSS helpers ──────────────────────────────────────────────────────────
    private string PillClass(string filter) =>
        statusFilter == filter ? "filter-pill active" : "filter-pill";

    private string RowClass(ItemCategoryModel item) =>
        item.Active ? "list-item" : "list-item item-inactive";

    private string TickClass(bool active) =>
        active ? "custom-tick-checkbox checked" : "custom-tick-checkbox";

    private string BadgeClass(bool active) =>
        active ? "status-badge badge-active" : "status-badge badge-inactive";

    private string FormTickClass =>
        currentModel.Active ? "custom-tick-checkbox checked" : "custom-tick-checkbox";

    private string ToastClass =>
        notificationType == "error" ? "alert-toast alert-error" : "alert-toast alert-success";

    private string ToastIcon =>
        notificationType == "error" ? "fa-circle-exclamation" : "fa-circle-check";

    // ── Lifecycle ─────────────────────────────────────────────────────────────
    protected override async Task OnInitializedAsync()
    {
        await LoadDataAsync();
    }

    private async Task LoadDataAsync()
    {
        isLoading = true;
        try
        {
            var departmentsTask = ItemDepartmentService.GetDepartmentsAsync();
            var categoriesTask = ItemCategoryService.GetCategoriesAsync();

            await Task.WhenAll(departmentsTask, categoriesTask);

            departments = departmentsTask.Result ?? new();
            categories = categoriesTask.Result ?? new();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"[ItemCategory] Error: {ex.Message}");
            categories = new();
            ShowNotification("Failed to load item categories.", isError: true);
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private IEnumerable<ItemCategoryModel> FilteredCategories
    {
        get
        {
            var query = categories.AsEnumerable();

            if (!string.IsNullOrWhiteSpace(searchQuery))
            {
                var q = searchQuery.Trim();
                query = query.Where(d =>
                    (d.Code?.Contains(q, StringComparison.OrdinalIgnoreCase) ?? false) ||
                    (d.Name?.Contains(q, StringComparison.OrdinalIgnoreCase) ?? false) ||
                    (d.DepartmentName?.Contains(q, StringComparison.OrdinalIgnoreCase) ?? false));
            }

            if (!string.IsNullOrWhiteSpace(departmentFilter))
            {
                query = query.Where(d => string.Equals(d.DepartmentId, departmentFilter, StringComparison.OrdinalIgnoreCase));
            }

            if (statusFilter == "Active")
                query = query.Where(d => d.Active);
            else if (statusFilter == "Inactive")
                query = query.Where(d => !d.Active);

            return sortColumn switch
            {
                "Name" => sortAscending
                    ? query.OrderBy(d => d.Name).ThenBy(d => d.Code)
                    : query.OrderByDescending(d => d.Name).ThenBy(d => d.Code),
                "Department" => sortAscending
                    ? query.OrderBy(d => d.DepartmentName).ThenBy(d => d.Code)
                    : query.OrderByDescending(d => d.DepartmentName).ThenBy(d => d.Code),
                "Active" => sortAscending
                    ? query.OrderBy(d => d.Active).ThenBy(d => d.Name)
                    : query.OrderByDescending(d => d.Active).ThenBy(d => d.Name),
                _ => sortAscending
                    ? query.OrderBy(d => d.Code)
                    : query.OrderByDescending(d => d.Code)
            };
        }
    }

    private void SortBy(string column)
    {
        if (sortColumn == column)
            sortAscending = !sortAscending;
        else { sortColumn = column; sortAscending = true; }
    }

    private void ClearFilters() { searchQuery = ""; statusFilter = "All"; departmentFilter = ""; }

    private void OpenAddModal()
    {
        isEditing = false;
        formError = "";
        currentModel = new ItemCategoryModel { Id = "", Code = "", Name = "", DepartmentId = "", Active = true };
        showModal = true;
    }

    private void OpenEditModal(ItemCategoryModel item)
    {
        isEditing = true;
        formError = "";
        currentModel = new ItemCategoryModel
        {
            Id = item.Id,
            Code = item.Code,
            Name = item.Name,
            DepartmentId = item.DepartmentId,
            DepartmentName = item.DepartmentName,
            Active = item.Active,
            Sequence = item.Sequence,
            CreatedDateTime = item.CreatedDateTime
        };
        showModal = true;
    }

    private void CloseModal()
    {
        if (isSaving) return;
        showModal = false;
        formError = "";
    }

    private void ToggleFormActive() => currentModel.Active = !currentModel.Active;

    private async Task ToggleItemActive(ItemCategoryModel item)
    {
        item.Active = !item.Active;
        var (success, _) = await ItemCategoryService.SaveCategoryAsync(item);
        if (success)
            ShowNotification($"'{item.Name}' is now {(item.Active ? "Active" : "Inactive")}.");
        else
        {
            item.Active = !item.Active;
            ShowNotification("Failed to update status.", isError: true);
        }
        StateHasChanged();
    }

    private async Task SaveCategory()
    {
        formError = "";

        if (string.IsNullOrWhiteSpace(currentModel.Name))
        {
            formError = "Category Name is required.";
            return;
        }

        var duplicate = categories.FirstOrDefault(d =>
            d.Id != currentModel.Id &&
            d.Name.Equals(currentModel.Name.Trim(), StringComparison.OrdinalIgnoreCase) &&
            string.Equals(d.DepartmentId, currentModel.DepartmentId ?? "", StringComparison.OrdinalIgnoreCase));
        if (duplicate != null)
        {
            formError = $"Category '{currentModel.Name.Trim()}' already exists in this department.";
            return;
        }

        isSaving = true;
        try
        {
            currentModel.Name = currentModel.Name.Trim();

            var (success, message) = await ItemCategoryService.SaveCategoryAsync(currentModel);
            if (success)
            {
                showModal = false;
                await LoadDataAsync();
                ShowNotification(isEditing ? $"Updated '{currentModel.Name}'." : $"Added '{currentModel.Name}'.");
            }
            else
            {
                formError = string.IsNullOrWhiteSpace(message) ? "Failed to save category." : message;
            }
        }
        catch (Exception ex)
        {
            formError = $"An error occurred: {ex.Message}";
        }
        finally
        {
            isSaving = false;
            StateHasChanged();
        }
    }

    private void RequestDelete(ItemCategoryModel item) { itemToDelete = item; showDeleteConfirm = true; }

    private void CancelDelete()
    {
        if (isDeleting) return;
        showDeleteConfirm = false;
        itemToDelete = null;
    }

    private async Task ConfirmDelete()
    {
        if (itemToDelete == null) return;
        isDeleting = true;
        try
        {
            var (success, message) = await ItemCategoryService.DeleteCategoryAsync(itemToDelete.Id);
            if (success)
            {
                showDeleteConfirm = false;
                var name = itemToDelete.Name;
                itemToDelete = null;
                await LoadDataAsync();
                ShowNotification($"Deleted '{name}'.");
            }
            else
            {
                ShowNotification(string.IsNullOrWhiteSpace(message) ? "Failed to delete." : message, isError: true);
            }
        }
        catch (Exception ex) { ShowNotification($"Error: {ex.Message}", isError: true); }
        finally { isDeleting = false; StateHasChanged(); }
    }

    private void ShowNotification(string msg, bool isError = false)
    {
        notificationMessage = msg;
        notificationType = isError ? "error" : "success";
        _ = AutoDismissNotification();
    }

    private async Task AutoDismissNotification()
    {
        await Task.Delay(4000);
        notificationMessage = "";
        await InvokeAsync(StateHasChanged);
    }

    private async Task TriggerToggleSidebar()
    {
        if (OnToggleSidebar.HasDelegate)
            await OnToggleSidebar.InvokeAsync();
    }
}
